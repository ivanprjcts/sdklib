==============
Authentication
==============

Login method
============

- `.login(self, **kargs)`

Do a `POST` request to `LOGIN_URL_PATH` using parameters passed to method.


Basic authentication
====================

Future work.


OAuth 2.0
=========

Future work.